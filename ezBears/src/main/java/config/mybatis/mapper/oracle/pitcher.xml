<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ez.ezBears.record.pitcher.model.PitcherDAO">
	<sql id="searchWhere">
		<where>
			<if test="searchKeyword!=null and searchKeyword!=''">
				${searchCondition} like '%' || #{searchKeyword} || '%'
			</if>
		</where>
	</sql>

	<select id="selectCheckId" parameterType="string"
		resultType="int">
		select count(*) from STAFF
		where STAFF_ID=#{staffId}
	</select>

	<select id="selectPwd" parameterType="String"
		resultType="string">
		select STAFF_PWD from STAFF
		where STAFF_ID=#{staffId}
	</select>

	<select id="selectStatus" parameterType="String"
		resultType="string">
		select STAFF_STATUS from STAFF
		where STAFF_ID=#{staffId}
	</select>
	
	<insert id="insertPitcher" parameterType="pitcherVo">
		<selectKey keyProperty="pitcherNo" order="BEFORE" resultType="int">
			select pitcher_seq.nextval from dual
		</selectKey>
		insert into pitcher(pitcher_no, recode_no, player_no, bb, so, ip, ha, rc, er, hr, era, staff_no, regdate)
		values(#{hitterNo}, #{recodeNo}, #{playerNo}, #{BB}, #{SO}, #{IP}, #{HA}, #{RC}, #{ER}, #{HR}, #{ERA} , #{staffNo}, #{regdate})
	</insert>
	
	<select id="selectByPlayerNo" resultType="map" parameterType="map">
		select * from pitcher_view
		where player_no = #{playerNo}
		and play_date = #{playDate}
	</select>
	
	<select id="selectPitcherByPlayerNo" resultType="pitcherVo" parameterType="int">
		select * from pitcher
		where player_no = #{playerNo}
	</select>
	
</mapper>