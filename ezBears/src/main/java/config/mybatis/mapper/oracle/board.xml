<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ez.ezBears.board.model.BoardDAO">

	<sql id="searchWhere">
		<where>
			<if test="searchKeyword!=null and searchKeyword!=''">
				${searchCondition} like '%' || #{searchKeyword} || '%'
			</if>
		</where>
	</sql>

	<select id="selectAllBoard"  parameterType="searchVo" resultType="map">
		select *
		from
		(
			select rownum as RNUM, A.* from
			(
				select * from board
				<include refid="searchWhere"></include>
				order by BOARD_NO desc
			)A
		)
	  <![CDATA[
      where RNUM> #{firstRecordIndex} 
      and RNUM<=#{firstRecordIndex} + #{recordCountPerPage}]]>		
	</select>
	
	<select id="boardAll" resultType="int">
		select boardnum from board
	</select>
	
	<select id="selectTotalCount" parameterType="SearchVo" resultType="int">
		select count(*) from board
		<include refid="searchWhere"></include>
	</select>
	
	<insert id="insertBoard" parameterType="boardVo">
		<selectKey keyProperty="boardNo" resultType="int" order="BEFORE">
			select BOARD_SEQ.nextval from dual
		</selectKey>
		insert into board(board_no,mem_no,board_title,board_content,boardnum)
		values(#{boardNo}, #{memNo}, #{boardTitle}, #{boardContent},#{randomnum})
	</insert> 
	
	<select id="randomNum" parameterType="int" resultType="int">
		select count(*) from board
		where boardnum=#{boardnum}
	</select>
	
</mapper>