<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ez.ezBears.temNotice.model.TeamNoticeDAO">

<!-- 	<insert id="insertTeamNotice" parameterType="teamNoticeVo">
		<selectKey keyProperty="teamNoticeNo" resultType="int">
			select teamNotice_seq.nextval from teamNotice
		</selectKey>
		insert into teamNotice(teamNoticeNo, memNo, myBoardNo, teamNoticeTitle, teamNoticeContent,fsize,fileName,originname)
		values()
	</insert> -->
</mapper>